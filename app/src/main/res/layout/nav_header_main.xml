<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@color/bottomlogo"
    android:gravity="left"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="88dp"
        android:layout_height="wrap_content"
        android:src="@drawable/logosmall"
        android:layout_gravity="left"
        />

    <TextView
        android:id="@+id/paniclabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/imageView"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="SOS Sistem OMBA Siaga"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="16dp"
        android:singleLine="true"
        android:textStyle="bold"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />
    <TextView
        android:id="@+id/polreslabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/imageView"
        android:layout_below="@id/paniclabel"
        android:text="Polres Omba"
        android:textColor="@color/maroon"
        android:textSize="13dp"
        android:textStyle="bold"
        android:singleLine="true"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/imageView"
        android:layout_below="@id/polreslabel"
        android:text="Polda Omba"
        android:textSize="13dp"
        android:textStyle="bold"
        android:textColor="@color/maroon"/>

</RelativeLayout>
